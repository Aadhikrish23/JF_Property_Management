export class NotificationsService {
  async list(filters: any): Promise<any> {
    // Placeholder: Fetch notifications log from database
    return [];
  }

  async markAsRead(id: string): Promise<any> {
    // Placeholder: Update notification read state to true
    return null;
  }
}
